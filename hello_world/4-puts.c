#include <stdio.h>

/*
 * Fonction qui permet d'afficher un message sp√cifique et un retour a la ligne automatique gr√ce a puts
 */

int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
