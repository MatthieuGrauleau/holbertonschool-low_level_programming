#include "main.h"

/**
 * _isdigit - entry point
 *
 * @c: digit
 *
 * Description: Checks for a digit 0 to 9
 *
 * Return: 0 for success.
 */

int _isdigit(int c)
{
	if (c >= '0' && c <= '9')
	{
		return (1);
	} {
		return (0);
	}
}
